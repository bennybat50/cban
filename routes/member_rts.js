const express=require('express');
const router=express.Router();
const homeController=require('../controllers/home');
const loginController=require('../controllers/login');
const newsController=require('../controllers/news');
const profileController=require('../controllers/profile');
const projectsController=require('../controllers/projects');
const aProjectController=require('../controllers/view_project');
const passwordController=require('../controllers/password');
const membersController=require('../controllers/members');
const donationController=require('../controllers/donation');
const subscriptionController=require('../controllers/subscription');
const givingController=require('../controllers/giving');
const causeController=require('../controllers/causes');
const referralsController=require('../controllers/referrals');
router.get('/', homeController.getMemHome);
router.get('/logout', loginController.logoutUser);
router.get('/news', newsController.getMemNews);
router.get('/view-news/:id', newsController.viewNews);
router.get('/profile', profileController.getMemProfile);
router.post('/profile', profileController.postProfileUpdate);
router.get('/password', passwordController.getMemPassword);
router.post('/password', passwordController.sendNewPassword);
router.get('/projects', projectsController.getMemProjects);
router.get('/active-projects', projectsController.getAMemProjects);
router.get('/view-project/:id', aProjectController.getAProject);
router.get('/members', membersController.getAllMemebers); 
router.get('/unverified', membersController.getUnverified); 
router.get('/donations', donationController.getMemDonations); 
router.get('/subscription', subscriptionController.getMemSubscriptions); 
router.get('/giving', givingController.getMemGiving); 
router.get('/causes', causeController.getMemCauses); 
router.get('/active-causes', causeController.getAMemCauses); 
router.get('/referrals', referralsController.getMemReferrals); 
router.get('/pending-referrals', referralsController.getMemPendingReferrals); 
router.get('/verify', referralsController.verifyMember); 
module.exports=router;
